// 22% - NOT FINISHED 

if ( len == *in){
    return -1 ;
}
else if ( (*in) == 0 ){
    (*first) = 0 ;
    (*last) =1 ;
    (*in) = 1  ;
    (*buf ) = d ;
}
(*last )++ ;
(*buf) = d ;
(*in) ++ ;
return 0 ;




if (first == NULL || last == NULL || buf == NULL || in == NULL){
    return NULL ; 
}
if(len == 0 ){
    return NULL ;
    
}
return *buf;